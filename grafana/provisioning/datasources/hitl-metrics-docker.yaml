apiVersion: 1

datasources:
  - name: hitl-metrics
    uid: hitl-metrics
    type: frser-sqlite-datasource
    access: proxy
    isDefault: false
    jsonData:
      path: /var/lib/grafana/hitl-metrics.db
    editable: true
